import React from "react";

const TopBar = () => {

  return (
    <div class='flex flex-row flex-grow bg-yellow-400 justify-end hover:text-white p-4'>
      <button>Login</button>
    </div>
  );
};

export default TopBar